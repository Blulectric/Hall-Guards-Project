using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI_NoticeIcon : MonoBehaviour
{

    public List<GameObject> enemiesNoticed;

    // Start is called before the first frame update
    void Start()
    {
        //numbersList.Add(gameObject);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
